根目录作为 ESM 单包入口，由 vite.config.js 统一装配：启用 @vitejs/plugin-react、将 CJS/ESM 混用的 stats.js/use-sync-external-store/lenis 等依赖纳入 optimizeDeps.include 以避免运行时崩溃；在 build.rollupOptions.output.manualChunks 中将 three/@react-three、framer-motion/gsap、react/react-dom 分别拆分为 three-vendor、animation-vendor、react-vendor 三个 chunk，配合 terser 压缩与 drop_console/drop_debugger 生产优化。index.html 仅保留 <div id="root"></div> 与 <script type="module" src="/src/main.jsx"></script>，实际路由与页面渲染下沉到 app_shell（全局常量与顶层容器）和 page_sections（黑洞主题首页），二者通过 npm 依赖共享 react、three、@react-three/fiber、framer-motion、gsap、lenis 等库，无需跨子模块显式通信。部署脚本 predeploy 先执行 vite build，再由 gh-pages -d dist 推送 dist 目录。