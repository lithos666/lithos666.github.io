- 全局配置以命名空间对象形式从 `constants.js` 统一导出（如 `COVER_FLOW`、`CARD_STYLE`、`CURSOR`），组件内通过解构常量属性访问，禁止散落的魔法数字。
- 首屏可见组件直接 import，首屏以下的大体积区块一律使用 `React.lazy(() => import(...))` 配合 `Suspense fallback={<SectionSkeleton />}` 进行懒加载。
- 滚动相关状态通过 `framer-motion` 的 `useScroll` + `useSpring` 驱动，而非手动监听 `scroll` 事件计算样式。